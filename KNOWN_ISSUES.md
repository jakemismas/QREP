# Known issues

Criteria that could not be met after three documented distinct attempts. Each
entry records the criterion, the three attempts with their actual numbers, and
the xfail-marked test that references it. The `APPROACH FAILED:` comments on
the sprint issues carry the full history behind each entry.

No entries yet.
