# reference/

Drop real quilt photos here to activate the stretch phase (Phase 6) of the
build. Image files in this directory are gitignored by default; commit small
ones deliberately with `git add -f`.
